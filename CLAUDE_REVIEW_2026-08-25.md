# Claude 版精進盤點 · 2026-08-25

對 `wegoliao/performance-accumulation-dashboard` 在 commit `d1805cd` 的獨立稽核。
公開版：<https://wegoliao.github.io/performance-accumulation-dashboard/claude/>

每一項都可用 repo 內的 `scripts/` 與 `inputs/` 重跑驗證。本文不產生交易建議、不含委託能力、不改動 `inputs/actual_fills.csv`。

## 摘要

| 指標 | 值 |
|---|---:|
| 8/24 當日報酬中來自估值基準切換而非價格的部分 | +0.27pp（NT$5,372） |
| 仍未解的自動化斷點 | 1 處（#02） |
| 實際日報酬樣本數 / 風險統計門檻 | 10 / 20 |
| 核心 sleeve 重建函式的測試覆蓋 | 0 |

---

## P0 · 會讓數字說謊，且今天就會發生

### 01 曲線最後一天換了估值基準

`build_four_strategy_actual()` 在 `day == snapshot_asof` 時直接採用券商快照的 `current_value_twd`；其餘所有日期走 `estimated_liquidation_value()`，扣掉 0.1425% 手續費與 0.3% 證交稅。同一條曲線上有兩種基準。

證據：

```text
8/21 淨值基準             100.2168
8/24 依現行程式（毛）      101.4223  -> 當日 +1.2029%
8/24 若淨值一致            101.1537  -> 當日 +0.9349%
差                                     +0.2680% of NAV
```

累計報酬同樣受影響：`+1.42% -> +1.15%`。

快照確實是毛值：`1,218,013 = 成本 1,177,906 + 未實現 40,107`。而 diagnostics 卻標為 `OWNER_SNAPSHOT_NET_LIQUIDATION`，標籤與事實不符。

**今天 16:30 排程用官方收盤重建時，快照覆寫消失、基準切回淨值 —— 就算價格完全不動，曲線也會憑空掉約 0.27%。**

修法：整條曲線只用一種基準，並且兩條都出（`GROSS_MARK` 與 `NET_LIQUIDATION`）。券商快照降級為對帳用途，不參與估值。

### 02 每日自動 commit 不會觸發公開頁重新部署

`daily-close.yml` 用預設 `GITHUB_TOKEN` push；GitHub 明訂由 `GITHUB_TOKEN` 推出的 commit 不會再觸發其他 workflow。而 `pages.yml` 只在 push 到 `index.html` 時啟動。

證據：bot commit `907a623`、`c361d7b` 都修改了 `index.html`，但 workflow 清單裡沒有任何一次由這兩個 commit 觸發的 Pages 部署。目前所有成功部署都來自人工 push 或 `workflow_dispatch`。

修法：把 build 與 deploy 合成同一個 job，或在 daily-close 末尾以 `gh workflow run pages.yml` 顯式觸發。

### 03 8/25 的進出訊號只存在本機 — 稽核期間已修復

`latest_strategy_signals.csv`、`four_strategy_daily_signals.xlsx` 等 3 個未追蹤檔與 5 個已修改檔尚未 commit，線上最新仍是 `d1805cd`。`2637 慧洋-KY 進 @97.1`、`2646 星宇航空 出` 這兩筆計畫訊號在公開頁看不到。

已修復：稽核進行期間由 commit `e4c65cb`、`3cd918a` 推上。留在這裡是因為根因還在 —— 訊號入庫仍是本機動作，下一次一樣會停在本機。

---

## P1 · 稽核性與可重現性

### 04 沒有 .gitattributes，每天整檔改寫 CSV

本機 `core.autocrlf=true`、repo 無 `.gitattributes`。Linux runner 寫 LF、Windows 檢出寫 CRLF，於是 bot commit `c361d7b` 對 `price_history.csv` 產生 **4,115 行全刪全增**，實際只多了一天資料。「今天加了哪幾筆收盤」無法用 diff 回答。

修法：加 `*.csv text eol=lf`，做一次 renormalize。

### 05 所有公開數字的來源函式，測試覆蓋為零

10 個測試全數通過，但沒有一個碰到 `build_four_strategy_actual()`。既有測試涵蓋快照對帳、TWR、短樣本不造假、fail-closed 狀態 —— 都在 sleeve 重建之前的世代。第 01 項的基準切換就在這個函式裡，綠燈完全沒有察覺。

修法：至少三個測試 —— 現金＋部位守恆、賣超持股 fail-closed、整條曲線估值基準一致（最後這條會直接抓到 #01）。

### 06 日期與個股硬編碼在程式裡

`HOLDINGS_PATH = "holdings_snapshot_2026-08-24.csv"`、`if row["stock_code"] != "2886"`（兩處）、`EXPECTED_CARD_MEMBERS_LATEST` 的成員數。下一份快照送進來就得改 Python。

修法：快照改用「最新一份」解析；未歸屬部位改由獨立的 `unassigned.csv` 宣告；成員數從來源卡自己數。

---

## P2 · 口徑缺口：系統性偏移，不是隨機誤差

### 07 股息完全未處理

8/11–8/24 期間有 5 檔出現單日 ≤ −6% 跳空：`1590`（−7.8%）、`1709`（−6.9%）、`2408`（−6.6%）、`3006`（−9.2%）、`6672`（−7.0%）。目前的資料結構無法區分除權息與真跌。台股 7–9 月是除息旺季，持股含航運與電子零組件，只用收盤價等於系統性低估。

修法：加 `corporate_actions.csv`（除息日、現金股利、除權比例），估值用還原價並在頁面標示調整日。

### 08 「理論」用的是策略卡表頭，不是可投資的權益曲線

`strategy_card_returns.csv` 是「等權持有成員」的顯示報酬，沒有換股成本、部位權重、現金拖累與進出時點。已知矛盾：8/24 YOY 表頭 +3.7%，六檔可見成員平均 +4.33%，差 0.63pp。

修法：用 Quant_Grill_Lab 既有的 FinLab 資料，以相同 sleeve 規則（NT$50 萬、同樣費稅、同樣進出日）重跑成真正的 `strategy_nav.csv`。

### 09 NT$653 成本口徑差沒有歸因

成交簿在庫實付 `NT$1,178,519`，快照在庫成本（排除 2886）`NT$1,177,866`。已誠實揭露，但沒說是哪一筆造成的。逐檔比對即可落地。

### 10 樣本只有 10 天，行情歷史只有 242 天

實際日報酬 10 筆、門檻 20 筆，最快 9 月中才有第一個 Sharpe。`price_history.csv` 自 2025-08-25 起算共 242 個交易日，連一年風險統計都撐不起來。

修法：短期把「還要幾天才會出數字」寫在頁面上；中期行情歷史往回補三年。

---

## P3 · 真正的精進：現在沒有、但值得建

### 11 訊號→成交的履約落差帳（最高價值）

現在有「理論卡說什麼」與「帳戶最後值多少」，中間那一段 —— 訊號價到實際成交價的滑價 —— 是空的，而那正是「策略能不能實現」的全部答案。

料已就位：`latest_strategy_signals.csv` 有 `entry_price`，`actual_fills.csv` 有 `fill_price`。

每筆訊號一列：訊號日、訊號價、次日開盤、實際成交價、成交延遲天數、滑價（bp）、未成交原因。今天的 `2637 慧洋-KY 明開進 @97.1` 就是第一筆樣本，明天即可結案。累積 30 筆後，第一次會知道「策略卡報酬要打幾折」，而且是用自己的成交證明的。

### 12 容量欄位：零股單量 vs 成交量

sleeve 各 NT$50 萬，實際成交出現 63、261、305、347 股這類零股。零股流動性遠低於整股，是規模放大先撞到的牆，目前頁面看不到。

修法：每檔加 20 日均量、本次股數佔均量比、以及在不超過 X% 參與率下 sleeve 可放大的金額。

### 13 四策略表缺 benchmark 欄

`benchmark_nav.csv` 已有 0050 與加權指數到 8/24，但策略比較表只有「實際／理論／差距」。加一欄同期 0050 與一欄超額，成本幾乎為零。

### 14 毛／淨雙軌，已實現／未實現分離

現在一個數字混了三件事：價格變動、尚未發生的賣出費稅、3702 的已實現虧損。四個 sleeve 若全數平倉，未發生的賣出費稅合計約 `NT$5,372`（投信 1,850／YOY 1,878／融資 254／突破 1,390）。

修法：四欄並列 —— 毛報酬、淨可變現報酬、已實現損益、未實現損益。

### 15 把資料契約與展示層拆開

`build_dashboard.py` 1,422 行同時負責讀檔驗證、財務計算與 HTML 拼接。#01 之所以難發現，就是因為它藏在一段組畫面的迴圈裡。

修法：中間插一層 `dashboard_state.json` —— 計算只產生它，HTML 只讀它。狀態檔可被測試、可被 diff。

---

## 修復順序

| 順序 | 項目 | 代價 | 拿到什麼 |
|---:|---|---|---|
| — | #03 推上 8/25 訊號 | 已完成 | 公開頁與本機一致 |
| 1 | #02 讓每日 commit 觸發部署 | 改 5 行 YAML | 自動化真的自動 |
| 2 | #01 統一估值基準 | 1 個函式 ＋ 1 個測試 | 今日報酬不再多出 0.27pp |
| 3 | #04 .gitattributes | 1 行 ＋ 一次 renormalize | 每日行情 diff 可稽核 |
| 4 | #05 sleeve 重建測試 | 半天 | #01 那類錯誤自己現形 |
| 5 | #11 履約落差帳 | 一天 | 理論要打幾折，有自己的證據 |
| 6 | #07 除權息還原 | 一天 | 報酬不再系統性低估 |
| 7 | #08 真正的理論曲線 | 數天 | 實際 vs 理論變成可比 |

## 已經做對、不要動的部分

- 輸入契約 fail-closed：日期格式、負股數、賣超持股都直接擋下，不會靜默算下去。
- 樣本不足就顯示 N/A。10 筆日報酬不硬算 Sharpe，是這個 repo 最有價值的紀律。
- 2886 的 1 股標為未歸屬而不硬塞進某個策略。
- YOY 表頭與成員平均矛盾時原樣保留兩個數字。
- NT$653 成本口徑差主動揭露在畫面上。
- 建置產物有 sha256 receipt，整條支線無券商登入、無下單能力。

## 重跑驗證

```powershell
..\.venv\Scripts\python.exe -m pytest -q tests
..\.venv\Scripts\python.exe scripts\build_dashboard.py
```
